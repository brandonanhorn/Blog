# GitHub Pages publishing note

This repository's new static site is in `/docs`.

For GitHub Pages, configure the repository to publish from:
- Branch: `main` (or your default branch)
- Folder: `/docs`

The archive can be regenerated by running:

```bash
python3 scripts/build_archive.py
```
